name: flutter_moor_tutorial
description: A new Flutter project.


publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  moor_flutter: ^3.0.0
  provider: ^4.1.3
  flutter_slidable: ^0.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^3.1.0
  build_runner:

flutter:
  uses-material-design: true

 